Install "virtualenv"

create a virtuel env using command:
virtualenv venv

put libs in requrirements.txt

make sure pip freeze gives nothing

the run

pip install -r requrirements.txt

the run your script.

title to your x and y axis of graph

doc strings with params and return descrtiption

write unit tests